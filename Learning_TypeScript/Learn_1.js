let person_name_str = "KimJiWoo"; // 문자열
let person_name_array = ["KimJiWoo"]; // 문자열로 이루어진 array
let person_name_union_array = ["KimJiWoo", 12, "HI"]; // 문자열과 숫자로 이루어진 array
let person_name_obj = { name: "KimJiWoo" }; // 문자열로 이루어진 object
let person_name_union_obj_str = { name: "KimJiWoo" }; // 문자열과 숫자로 이루어진 object
let person_name_union_obj_num = { name: 123 }; // 문자열과 숫자로 이루어진 object
let person_name_obj_2 = { age: 27 }; // name 속성이 안들어올수도 있다는 object
let person_name_str_num = "KimJiWoo"; // 문자열과 숫자
let person_name_any = "Kim" + 23; // 모든 타입 가능
let person_name = "KimJiWoo";
function typeFunc(value) {
    return value * 5;
}
function typeFunc_2(value) {
    return value * 5;
}
let jiwoo_info = ["jiwoo", true];
let obj_example = { "name": "JiWoo", "age": "27" };
let project_example = {
    member: ['kim', 'park'],
    days: 30,
    started: true,
};
