type project_type = {
    member: string[],
    days: number,
    started: boolean
}

let project :project_type = {
    member : ['kim', 'park'],
    days : 30,
    started : true,
  }
  

console.log(project)

